---
quick_id: 260721-vo3
status: in_progress
date: 2026-07-21
description: 移除已弃用的 Plane 代码与专属 Docker 资源，并部署自写甘特图本地服务
---

# Quick Task 260721-vo3 Plan

## Task 1: Retire Plane-only runtime and integration code

- Delete the `plane-selfhost/` deployment, Plane health/export/write/progress scripts, Plane-only skill, and active Plane operation docs.
- Remove Plane-only tests and references while preserving historical `.planning/` records and all Gantt data/snapshots.
- Remove only Docker resources carrying Compose project label `plane-app`, then remove the exact images previously used by those containers.

Verify: no `plane-app` containers, volumes, or network remain; no active Plane deployment/integration entrypoint remains in tracked source.

## Task 2: Make the custom Gantt the standalone local product

- Rebrand user-facing launcher/docs/package names from Plane Demand Hub Gantt to Delivery Gantt.
- Move the local source service to `127.0.0.1:8090` and keep the existing snapshot/autosave compatibility identifiers unchanged.
- Keep other Docker projects and the user's untracked `.codex/state/` untouched.

Verify: focused Gantt tests pass and the source launchers consistently target port 8090.

## Task 3: Deploy and validate

- Start the Gantt server as a background local process.
- Validate HTTP 200 for `/gantt.html`, validate the portable snapshot API, and confirm persisted task/event counts are still available.
- Record the completed quick task in `.planning/STATE.md` and summarize destructive actions explicitly.

Verify: `http://127.0.0.1:8090/gantt.html` serves the custom Gantt and Plane Docker inventory is empty.
