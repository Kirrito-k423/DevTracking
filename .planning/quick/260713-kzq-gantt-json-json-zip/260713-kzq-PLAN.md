---
status: ready
created: 2026-07-13
quick_id: 260713-kzq
---

# Quick Task 260713-kzq: 修复公开 Gantt 的 JSON 导入导出

## Scope

- 保留本地桌面版的 ZIP 迁移包能力。
- 让没有后端 API 的公开静态页面导出的 JSON 可被同一页面导入。
- 修正文件选择与拖放逻辑，避免一次选择触发重复导入。
- 更新可移植快照并重新部署公开站点。

## Plan

1. 在 Gantt 生成模板中支持 JSON 变化集与 ZIP 迁移包两种导入路径，并移除重复导入调用。
2. 重新生成当前交付与可移植 Gantt 页面，保留用户尚未提交的最新快照数据。
3. 验证 JSON 导入处理、ZIP 导入保留、生成页面语法和公开版构建，然后重新发布。

## Verification

- 导出的 JSON 变化集可在浏览器中直接恢复任务和事件快照。
- 本地服务仍接受 ZIP 迁移包。
- 文件选择和拖放均只调用一次导入。
- 当前可移植看板和公开站点构建成功。
