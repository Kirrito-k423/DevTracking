---
status: ready
created: 2026-07-08
quick_id: 260708-d1x
---

# Quick Task 260708-d1x: Fix Gantt stale fade event inheritance and local task coverage

## Scope

- Parent task stale fading should treat descendant task events as activity for the parent.
- Locally added task bars should participate in stale fading even before they have explicit event markers.
- Existing event marker rendering should remain row-local; child events should not be duplicated onto parent rows.

## Tasks

1. Refactor the Gantt freshness calculation in `scripts/build-delivery-dashboard.py` so it can walk the task subtree for stale color decisions.
2. Add local-task fallback activity dates for tasks created through the HTML add button when no explicit event exists.
3. Regenerate the Gantt export and verify the new rules with a browser smoke test.

## Verification

- Python compile passes.
- Gantt export regenerates successfully.
- Generated Gantt JavaScript parses.
- Browser smoke test confirms child events refresh parent opacity and local tasks without explicit events fade from their start date.
