---
quick: 260723-p3e
date: 2026-07-23
status: complete
commit: f73a26c
---

# Gantt 完成任务自动隐藏总结

## 完成内容

- 默认隐藏状态为完成、进度达到 100% 或存在完成时间的任务。
- 沿父任务链过滤，完成父任务会同时隐藏全部后代任务。
- 隐藏任务对应的时间轴行、事件标记和未来事件预警同步消失。
- 工具栏新增“全部显示”按钮；开启后展开完整任务树，按钮切换为“隐藏已完成”。
- 编辑任务字段并标记完成后立即关闭详情并从默认视图消失。
- 保留原有 schema、localStorage 标识、自动保存、附件和迁移数据。

## 验证

- `python3 -m unittest discover -s tests -v`：8 项通过。
- 内联 JavaScript 语法检查通过。
- `http://127.0.0.1:8090/gantt.html`：HTTP 200。
- 真实浏览器：默认 104 行，全部显示 105 行，切回后恢复 104 行；按钮状态和文案正确，控制台无错误。
- 运行目录的两个 Gantt HTML 与仓库文件 SHA-256 一致；未覆盖 JSON、自动保存或附件。
